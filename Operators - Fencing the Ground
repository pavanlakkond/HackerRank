#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a,b,n,n1;
    scanf("%d\n%d",&a,&b);
    n=a*b;
    n1=2*(a+b);
    printf("The required length is %d m\n",n1);
    printf("The required area of carpet is %d sqm",n);
    return 0;
}
